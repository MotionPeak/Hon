# macOS
.DS_Store

# Node
node_modules/
*.log
npm-debug.log*

# Local financial data — NEVER commit
*.db
*.db-wal
*.db-shm
data/

# Secrets — the deployment token. Commit .env.example, never .env.
.env
.env.local

# Local Claude Code session scratch (worktrees, memory) — never track
.claude/

# Internal dev docs — kept on disk locally, not tracked in the repo.
# README.md is the only doc the repo ships.
/CLAUDE.md
/HANDOFF.md
/PROJECT-RULES.md
/docs/
