diff --git a/node_modules/israeli-bank-scrapers/lib/scrapers/base-beinleumi-group.js b/node_modules/israeli-bank-scrapers/lib/scrapers/base-beinleumi-group.js
index 4e821fa..f3cd2a4 100644
--- a/node_modules/israeli-bank-scrapers/lib/scrapers/base-beinleumi-group.js
+++ b/node_modules/israeli-bank-scrapers/lib/scrapers/base-beinleumi-group.js
@@ -58,9 +58,14 @@ function createLoginFields(credentials) {
   }];
 }
 function getAmountData(amountStr) {
-  let amountStrCopy = amountStr.replace(_constants.SHEKEL_CURRENCY_SYMBOL, '');
-  amountStrCopy = amountStrCopy.replaceAll(',', '');
-  return parseFloat(amountStrCopy);
+  // Hon patch: the FIBI page wraps amounts in Unicode directional marks and
+  // may put the minus sign at the end, which the original parseFloat could not
+  // handle. Strip everything but digits/dot/minus and treat any minus as sign.
+  const cleaned = String(amountStr == null ? '' : amountStr).replace(/[^0-9.\-]/g, '');
+  const negative = cleaned.indexOf('-') >= 0;
+  const num = parseFloat(cleaned.replace(/-/g, ''));
+  if (Number.isNaN(num)) return NaN;
+  return negative ? -num : num;
 }
 function getTxnAmount(txn) {
   const credit = getAmountData(txn.credit);
@@ -73,7 +78,10 @@ function convertTransactions(txns, options) {
     const convertedAmount = getTxnAmount(txn);
     const result = {
       type: _transactions2.TransactionTypes.Normal,
-      identifier: txn.reference ? parseInt(txn.reference, 10) : undefined,
+      // Hon patch: keep the reference as its raw string. parseInt dropped any
+      // non-numeric reference to NaN, which then collapsed every such
+      // transaction onto a single dedup key downstream.
+      identifier: txn.reference ? txn.reference : undefined,
       date: convertedDate,
       processedDate: convertedDate,
       originalAmount: convertedAmount,
