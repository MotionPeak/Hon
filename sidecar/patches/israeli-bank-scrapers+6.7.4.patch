diff --git a/node_modules/israeli-bank-scrapers/lib/scrapers/base-beinleumi-group.js b/node_modules/israeli-bank-scrapers/lib/scrapers/base-beinleumi-group.js
index 4e821fa..f3cd2a4 100644
--- a/node_modules/israeli-bank-scrapers/lib/scrapers/base-beinleumi-group.js
+++ b/node_modules/israeli-bank-scrapers/lib/scrapers/base-beinleumi-group.js
@@ -58,9 +58,14 @@ function createLoginFields(credentials) {
   }];
 }
 function getAmountData(amountStr) {
-  let amountStrCopy = amountStr.replace(_constants.SHEKEL_CURRENCY_SYMBOL, '');
-  amountStrCopy = amountStrCopy.replaceAll(',', '');
-  return parseFloat(amountStrCopy);
+  // Hon patch: the FIBI page wraps amounts in Unicode directional marks and
+  // may put the minus sign at the end, which the original parseFloat could not
+  // handle. Strip everything but digits/dot/minus and treat any minus as sign.
+  const cleaned = String(amountStr == null ? '' : amountStr).replace(/[^0-9.\-]/g, '');
+  const negative = cleaned.indexOf('-') >= 0;
+  const num = parseFloat(cleaned.replace(/-/g, ''));
+  if (Number.isNaN(num)) return NaN;
+  return negative ? -num : num;
 }
 function getTxnAmount(txn) {
   const credit = getAmountData(txn.credit);
@@ -73,7 +78,10 @@ function convertTransactions(txns, options) {
     const convertedAmount = getTxnAmount(txn);
     const result = {
       type: _transactions2.TransactionTypes.Normal,
-      identifier: txn.reference ? parseInt(txn.reference, 10) : undefined,
+      // Hon patch: keep the reference as its raw string. parseInt dropped any
+      // non-numeric reference to NaN, which then collapsed every such
+      // transaction onto a single dedup key downstream.
+      identifier: txn.reference ? txn.reference : undefined,
       date: convertedDate,
       processedDate: convertedDate,
       originalAmount: convertedAmount,
diff --git a/node_modules/israeli-bank-scrapers/lib/scrapers/base-isracard-amex.js b/node_modules/israeli-bank-scrapers/lib/scrapers/base-isracard-amex.js
index 0cd2096..0350347 100644
--- a/node_modules/israeli-bank-scrapers/lib/scrapers/base-isracard-amex.js
+++ b/node_modules/israeli-bank-scrapers/lib/scrapers/base-isracard-amex.js
@@ -219,9 +219,22 @@ async function fetchAllTransactions(page, options, companyServiceOptions, startM
       combinedTxns[accountNumber].push(...toBeAddedTxns);
     });
   });
+  // Hon patch: Isracard/Amex expose no per-card balance, so each card's
+  // outstanding is computed from its own scheduled-but-not-yet-billed
+  // transactions — the same data the scraper already pulls. Sums every
+  // transaction whose processedDate (the billing date) is today or later;
+  // the result is negative (debt) and is the amount Isracard/Amex will
+  // still charge that card.
+  const today = new Date().toISOString().slice(0, 10);
   const accounts = Object.keys(combinedTxns).map(accountNumber => {
+    const balance = combinedTxns[accountNumber].reduce((sum, txn) => {
+      const day = (txn.processedDate || '').slice(0, 10);
+      if (day && day >= today) sum += txn.chargedAmount || 0;
+      return sum;
+    }, 0);
     return {
       accountNumber,
+      balance,
       txns: combinedTxns[accountNumber]
     };
   });
diff --git a/node_modules/israeli-bank-scrapers/lib/scrapers/discount.js b/node_modules/israeli-bank-scrapers/lib/scrapers/discount.js
index 630d930..b2e041f 100644
--- a/node_modules/israeli-bank-scrapers/lib/scrapers/discount.js
+++ b/node_modules/israeli-bank-scrapers/lib/scrapers/discount.js
@@ -88,7 +88,10 @@ async function navigateOrErrorLabel(page) {
 }
 function getPossibleLoginResults() {
   const urls = {};
-  urls[_baseScraperWithBrowser.LoginResults.Success] = [`${BASE_URL}/apollo/retail/#/MY_ACCOUNT_HOMEPAGE`, `${BASE_URL}/apollo/retail2/#/MY_ACCOUNT_HOMEPAGE`, `${BASE_URL}/apollo/retail2/`];
+  // Hon patch: Discount bumps the retail app path over time (retail -> retail2
+  // -> retail3 ...). A literal URL list silently breaks on each bump and the
+  // login is misreported as UNKNOWN_ERROR, so match any retail<N> homepage.
+  urls[_baseScraperWithBrowser.LoginResults.Success] = [/\/apollo\/retail\d*\/(#\/MY_ACCOUNT_HOMEPAGE)?$/];
   urls[_baseScraperWithBrowser.LoginResults.InvalidPassword] = [`${BASE_URL}/apollo/core/templates/lobby/masterPage.html#/LOGIN_PAGE`];
   urls[_baseScraperWithBrowser.LoginResults.ChangePassword] = [`${BASE_URL}/apollo/core/templates/lobby/masterPage.html#/PWD_RENEW`];
   return urls;
diff --git a/node_modules/israeli-bank-scrapers/lib/scrapers/max.js b/node_modules/israeli-bank-scrapers/lib/scrapers/max.js
index ab8e48d..074885c 100644
--- a/node_modules/israeli-bank-scrapers/lib/scrapers/max.js
+++ b/node_modules/israeli-bank-scrapers/lib/scrapers/max.js
@@ -289,9 +289,21 @@ class MaxScraper extends _baseScraperWithBrowser.BaseScraperWithBrowser {
   }
   async fetchData() {
     const results = await fetchTransactions(this.page, this.options);
+    // Hon patch: Max exposes no per-card balance, so each card's outstanding
+    // is computed from its own scheduled-but-not-yet-billed transactions —
+    // the same data the scraper already pulls. Sums every transaction whose
+    // paymentDate (mapped to `processedDate`) is today or later; the result
+    // is negative (debt) and is the amount Max will still charge that card.
+    const today = new Date().toISOString().slice(0, 10);
     const accounts = Object.keys(results).map(accountNumber => {
+      const balance = results[accountNumber].reduce((sum, txn) => {
+        const day = (txn.processedDate || '').slice(0, 10);
+        if (day && day >= today) sum += txn.chargedAmount || 0;
+        return sum;
+      }, 0);
       return {
         accountNumber,
+        balance,
         txns: results[accountNumber]
       };
     });
