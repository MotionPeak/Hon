# Copy to `.env` in this directory before `docker compose -f docker-compose.cd.yml up`.
# The bearer token that gates every API call — your only authentication.
# Use the SAME token your NAS already runs with (or generate a new one and
# update your bookmarks). Generate: node -e "console.log(require('crypto').randomUUID())"
HON_TOKEN=
